---
description: YASA 规则生成采用折中流程，并保留语义检查
alwaysApply: false
---
# YASA 规则生成与校对流程

## 折中流程（必须遵循）

1. **规则草稿来源**：可由 DeepSeek（`/api/generate-rule`）根据漏洞描述 + PoC 生成规则草稿。
2. **不直接采纳**：DeepSeek 输出视为草稿，不未经校对直接写入 `rules.json`。
3. **语义与格式校对**：在写入前必须做一次校对：
   - 结构符合现有 `rules.json`（如 `checkerIds`、`sources`、`sinks`）；
   - 使用基于语义的 TaintSource / FuncCallTaintSink 等定义，而非仅正则；
   - 字段完整、类型正确（如 `fsig`、`args`、`attribute`、`scopeFile`、`scopeFunc` 等）。

## 保留语义检查

- 实现或维护「规则生成」相关功能时，必须保留或提供**语义检查**能力：即对即将写入的规则做一次结构 + 语义校验，再落盘。
- 在 `yasa-api-agent` 中，若存在对 AI 生成规则的校验/审查步骤，不得移除；若尚未实现，新增规则写入逻辑时应加上校验（如 schema 校验或人工/AI 复核步骤）。

## 谁写规则

- **重要/复杂规则**：优先由 AI 助手（Cursor）根据 YASA 文档与现有 `rules.json` 直接编写，再合并进仓库。
- **批量/简单规则**：可用 DeepSeek 生成草稿，再由 AI 助手或人工做语义检查后写入。
