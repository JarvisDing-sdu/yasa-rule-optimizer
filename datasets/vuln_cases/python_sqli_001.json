{
  "case_id": "python_sqli_001",
  "language": "python",
  "vulnerability_type": "SQLInjection",
  "source_code_before": "import sqlite3\nfrom flask import Flask, request\n\napp = Flask(__name__)\n\n@app.route('/user')\ndef get_user():\n    user_id = request.args.get('id')\n    conn = sqlite3.connect('users.db')\n    cursor = conn.cursor()\n    query = f\"SELECT * FROM users WHERE id = {user_id}\"\n    cursor.execute(query)\n    result = cursor.fetchone()\n    return str(result)",
  "source_code_after": "import sqlite3\nfrom flask import Flask, request\n\napp = Flask(__name__)\n\n@app.route('/user')\ndef get_user():\n    user_id = request.args.get('id')\n    conn = sqlite3.connect('users.db')\n    cursor = conn.cursor()\n    query = \"SELECT * FROM users WHERE id = ?\"\n    cursor.execute(query, (user_id,))\n    result = cursor.fetchone()\n    return str(result)",
  "source_function_hints": [
    "request.args.get",
    "request.form.get",
    "request.json.get"
  ],
  "sink_function_hints": [
    "cursor.execute",
    "db.execute",
    "connection.execute"
  ],
  "notes": "SQL Injection: user input directly concatenated into SQL query"
}
