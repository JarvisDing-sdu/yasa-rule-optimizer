"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Parser = exports.LanguageType = exports.version = void 0;
const jsParser = __importStar(require("./frontend/javascript/parser"));
const javaParser = __importStar(require("./frontend/java/parser"));
const path = __importStar(require("path"));
var package_json_1 = require("../package.json");
Object.defineProperty(exports, "version", { enumerable: true, get: function () { return package_json_1.version; } });
var LanguageType;
(function (LanguageType) {
    LanguageType[LanguageType["LANG_JAVASCRIPT"] = 0] = "LANG_JAVASCRIPT";
    LanguageType[LanguageType["LANG_JAVA"] = 1] = "LANG_JAVA";
    LanguageType[LanguageType["OTHER"] = 2] = "OTHER";
})(LanguageType = exports.LanguageType || (exports.LanguageType = {}));
class Parser {
    opts;
    cachedParser;
    constructor(opts) {
        this.opts = opts || { language: LanguageType.LANG_JAVASCRIPT };
    }
    parse(content, opts) {
        const parser = this.prepareParser(content, opts);
        //TODO sanitize check return value
        return parser.parse(content, opts);
    }
    prepareParser(content, opts) {
        let langType = opts?.language || this.opts?.language;
        const filename = opts?.filename || this.opts?.filename;
        if (!langType && filename) {
            // find through extname of file
            const ext = path.extname(filename);
            if (['js', 'ts', 'mjs', 'cjs'].indexOf(ext) !== -1) {
                langType = LanguageType.LANG_JAVASCRIPT;
            }
            else if ('java'.indexOf(ext) !== -1) {
                langType = LanguageType.LANG_JAVA;
            }
            else { // default
                langType = LanguageType.LANG_JAVASCRIPT;
            }
        }
        switch (langType) {
            case LanguageType.LANG_JAVASCRIPT:
                return jsParser;
            case LanguageType.LANG_JAVA:
                return javaParser;
            default:
                throw new Error(`Language type: ${langType} not supported`);
        }
    }
}
exports.Parser = Parser;
//# sourceMappingURL=index.js.map