"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.parse = exports.appendLocMeta = exports.createTmpVariableDeclaration = exports.createTmpIdentifier = void 0;
// @ts-ignore
const babelCore = __importStar(require("@babel/core"));
const babelParser = __importStar(require("@babel/parser"));
const AST = __importStar(require("@babel/types"));
const UAST = __importStar(require("@ant-yasa/uast-spec"));
const uast_spec_1 = require("@ant-yasa/uast-spec");
const visitor = (opts) => ({
    EmptyStatement(node) {
        return UAST.noop();
    },
    File(node) {
        return visit(node.program, opts);
    },
    Program(node) {
        const body = node.body.map((stmt) => {
            return visit(stmt, opts);
        });
        // @ts-ignore
        const source = node.loc?.filename || '';
        return UAST.compileUnit(body, 'javascript', null, source, uast_spec_1.version);
    },
    Identifier(node) {
        return UAST.identifier(node.name);
    },
    PrivateName(node) {
        return UAST.identifier(node.id.name);
    },
    RegExpLiteral(node) {
        return UAST.literal(node.pattern, 'string');
    },
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    NullLiteral(node) {
        return UAST.literal(null, 'null');
    },
    StringLiteral(node) {
        return UAST.literal(node.value, 'string');
    },
    BooleanLiteral(node) {
        return UAST.literal(node.value, 'boolean');
    },
    NumericLiteral(node) {
        return UAST.literal(node.value, 'number');
    },
    BigIntLiteral(node) {
        return UAST.literal(node.value, 'number');
    },
    DecimalLiteral(node) {
        return UAST.literal(node.value, 'string');
    },
    TemplateLiteral(node) {
        if (node.quasis.length === 1) {
            return UAST.literal(node.quasis[0].value.raw, 'string');
        }
        return buildBinaryExpression(node.quasis, node.expressions, false);
        function buildBinaryExpression(quasis, expressions, exprProcess) {
            if (!exprProcess) {
                const quasi = quasis.shift();
                if (!quasi) {
                    return UAST.literal("", 'string');
                }
                if (quasi.tail) {
                    const quasiLit = UAST.literal(quasi.value.raw, 'string');
                    quasiLit.loc = quasi.loc;
                    return quasiLit;
                }
                const binaryExpression = UAST.binaryExpression('+', UAST.literal(quasi.value.raw, 'string'), buildBinaryExpression(quasis, expressions, true));
                binaryExpression.left.loc = quasi.loc;
                return binaryExpression;
            }
            else {
                const expr = expressions.shift();
                if (!expr) {
                    throw new Error('expr cannot be empty');
                }
                if (expressions.length == 0 && quasis.length == 0) {
                    return visit(expr, opts);
                }
                return UAST.binaryExpression('+', visit(expr, opts), buildBinaryExpression(quasis, expressions, false));
            }
        }
    },
    ExpressionStatement(node) {
        return UAST.expressionStatement(visit(node.expression, opts));
    },
    BlockStatement(node) {
        return UAST.scopedStatement(node.body.map((stmt) => {
            return visit(stmt, opts);
        }), null);
    },
    // with(x) { //code }
    //  to
    // {
    //    ...x;
    //    //code
    // }
    WithStatement(node) {
        const spreadElement = UAST.spreadElement(visit(node.object, opts));
        const body = visit(node.body, opts);
        return UAST.scopedStatement([
            UAST.expressionStatement(spreadElement),
            body
        ], null);
    },
    ReturnStatement(node) {
        return UAST.returnStatement(visit(node.argument, opts));
    },
    LabeledStatement(node) {
        return UAST.scopedStatement([visit(node.body, opts)], visit(node.label, opts));
    },
    BreakStatement(node) {
        return UAST.breakStatement(visit(node.label, opts));
    },
    ContinueStatement(node) {
        return UAST.continueStatement(visit(node.label, opts));
    },
    IfStatement(node) {
        return UAST.ifStatement(visit(node.test, opts), visit(node.consequent, opts), visit(node.alternate, opts));
    },
    SwitchStatement(node) {
        return UAST.switchStatement(visit(node.discriminant, opts), node.cases.map((c) => {
            return visit(c, opts);
        }));
    },
    SwitchCase(node) {
        return UAST.caseClause(visit(node.test, opts), UAST.scopedStatement(node.consequent.map((stmt) => {
            return visit(stmt, opts);
        })));
    },
    ThrowStatement(node) {
        return UAST.throwStatement(visit(node.argument, opts));
    },
    TryStatement(node) {
        let handlers;
        if (node.handler) {
            handlers = [visit(node.handler, opts)];
        }
        return UAST.tryStatement(visit(node.block, opts), handlers, visit(node.finalizer, opts));
    },
    CatchClause(node) {
        const body = visit(node.body, opts);
        const parameter = [];
        if (!node.param) {
            return UAST.catchClause(parameter, body);
        }
        else if (AST.isIdentifier(node.param)) {
            const visited = visit(node.param, opts);
            const decl = UAST.variableDeclaration(visited, null, false, UAST.dynamicType());
            decl.loc = visited.loc;
            parameter.push(decl);
            return UAST.catchClause(parameter, body);
        }
        else { //patternLike
            const varDecl = createTmpVariableDeclaration();
            const seq = _visitPatternLike(node.param, varDecl.id, opts);
            seq.expressions.push(body);
            parameter.push(varDecl);
            return UAST.catchClause(parameter, seq);
        }
    },
    WhileStatement(node) {
        return UAST.whileStatement(visit(node.test, opts), visit(node.body, opts), false);
    },
    DoWhileStatement(node) {
        return UAST.whileStatement(visit(node.test, opts), visit(node.body, opts), true);
    },
    ForStatement(node) {
        return UAST.forStatement(visit(node.init, opts), visit(node.test, opts), visit(node.update, opts), visit(node.body, opts));
    },
    ForInStatement(node) {
        return _visitRangeStatement(node, opts);
    },
    ForOfStatement(node) {
        // TODO 对于Map的考虑 e.g. for( const [a, b] of map )
        return _visitRangeStatement(node, opts);
    },
    FunctionDeclaration(node) {
        const res = _visitFunctionLike(node, opts);
        if (node.async) {
            res._meta.async = true;
        }
        if (node.generator) {
            res._meta.generator = true;
        }
        return res;
    },
    // VariableDeclaration 在两个场景下使用：
    // 1. 作为Statement
    // 2. 作为init存在于 For-like Statment
    // visit 只处理情况1， 情况2放在For like 里单独处理
    VariableDeclaration(node) {
        const resList = [];
        const attachList = [];
        node.declarations.forEach((d) => {
            attachList.push(visit(d, opts));
        });
        const seq = _flatSequence(attachList);
        if (seq.expressions.length === 1) {
            return seq.expressions[0];
        }
        else {
            return seq;
        }
    },
    VariableDeclarator(node) {
        if (AST.isIdentifier(node.id)) {
            let decl;
            // 新增 如果是ArrayExpression 则相应variable的vartype设置为arraytype
            if (node?.init?.type === "ArrayExpression") {
                decl = UAST.variableDeclaration(visit(node.id, opts), visit(node.init, opts), false, UAST.arrayType(UAST.identifier(node.id.name), UAST.dynamicType()));
                return UAST.sequence([decl]);
            }
            decl = UAST.variableDeclaration(visit(node.id, opts), visit(node.init, opts), false, UAST.dynamicType());
            decl.loc = node.loc;
            return UAST.sequence([decl]);
        }
        if (AST.isObjectPattern(node.id) || AST.isArrayPattern(node.id)) {
            if (!node.init) {
                throw new Error('init is expected when id is pattern');
            }
            const tmpVarDecl = createTmpVariableDeclaration(visit(node.init, opts), false);
            const sequence = _visitPatternLike(node.id, tmpVarDecl.id, opts);
            sequence.expressions.unshift(tmpVarDecl);
            return sequence;
        }
        // impossible
        throw new Error('VariableDeclarator id can not be any thing but id or pattern');
    },
    // Expressions
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    Super(node) {
        return UAST.superExpression();
    },
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    ThisExpression(node) {
        return UAST.thisExpression();
    },
    ArrowFunctionExpression(node) {
        const res = _visitFunctionLike(node, opts);
        if (node.async) {
            res._meta.async = true;
        }
        if (node.generator) {
            res._meta.generator = true;
        }
        return res;
    },
    YieldExpression(node) {
        return UAST.returnStatement(visit(node.argument, opts), true);
    },
    AwaitExpression(node) {
        return visit(node.argument, opts);
    },
    ArrayExpression(node) {
        const objProperties = [];
        node.elements.forEach((e, index) => {
            if (AST.isSpreadElement(e)) {
                objProperties.push(visit(e, opts));
            }
            else {
                if (e !== null) {
                    objProperties.push(UAST.objectProperty(UAST.literal(index, 'number'), visit(e, opts)));
                }
                else {
                    objProperties.push(UAST.objectProperty(UAST.literal(index, 'number'), UAST.literal(null, 'null')));
                }
            }
        });
        const uastNode = UAST.objectExpression(objProperties);
        uastNode._meta.isArray = true;
        return uastNode;
    },
    ObjectExpression(node) {
        const objProperties = [];
        node.properties.forEach((p) => {
            objProperties.push(visit(p, opts));
        });
        return UAST.objectExpression(objProperties);
    },
    ObjectProperty(node) {
        return UAST.objectProperty(visit(node.key, opts), visit(node.value, opts));
    },
    ObjectMethod(node) {
        const key = visit(node.key, opts);
        const value = _visitFunctionLike(node, opts);
        return UAST.objectProperty(key, value);
    },
    RecordExpression(node) {
        const objProperties = [];
        node.properties.forEach((p) => {
            objProperties.push(visit(p, opts));
        });
        return UAST.objectExpression(objProperties);
    },
    TupleExpression(node) {
        return UAST.tupleExpression(node.elements.map((e) => {
            return visit(e, opts);
        }));
    },
    FunctionExpression(node) {
        return _visitFunctionLike(node, opts);
    },
    UnaryExpression(node) {
        return UAST.unaryExpression(node.operator, visit(node.argument, opts), !node.prefix);
    },
    UpdateExpression(node) {
        return UAST.unaryExpression(node.operator, visit(node.argument, opts), !node.prefix);
    },
    BinaryExpression(node) {
        let operator = node.operator;
        if (operator === '===') {
            operator = '==';
        }
        return UAST.binaryExpression(node.operator, visit(node.left, opts), visit(node.right, opts));
    },
    AssignmentExpression(node) {
        if (AST.isObjectPattern(node.left) || AST.isArrayPattern(node.left)) {
            const varDecl = createTmpVariableDeclaration(visit(node.right, opts));
            varDecl.loc = node.right.loc;
            const seq = _visitPatternLike(node.left, varDecl.id, opts);
            seq.expressions.unshift(varDecl);
            return seq;
        }
        return UAST.assignmentExpression(visit(node.left, opts), visit(node.right, opts), node.operator, false);
    },
    LogicalExpression(node) {
        return UAST.binaryExpression(node.operator, visit(node.left, opts), visit(node.right, opts));
    },
    SpreadElement(node) {
        return UAST.spreadElement(visit(node.argument, opts));
    },
    RestElement(node) {
        return UAST.spreadElement(visit(node.argument, opts));
    },
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    ArgumentPlaceholder(node) {
        return UAST.noop();
    },
    MemberExpression(node) {
        return UAST.memberAccess(visit(node.object, opts), visit(node.property, opts), node.computed);
    },
    OptionalMemberExpression(node) {
        return UAST.conditionalExpression(visit(node.object, opts), UAST.literal(null, 'null'), UAST.memberAccess(visit(node.object, opts), visit(node.property, opts), node.computed));
    },
    //TODO
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    BindExpression(node) {
        throw new Error('BindExpression is not been implemented');
    },
    ConditionalExpression(node) {
        return UAST.conditionalExpression(visit(node.test, opts), visit(node.consequent, opts), visit(node.alternate, opts));
    },
    CallExpression(node) {
        return UAST.callExpression(visit(node.callee, opts), node.arguments.map((arg) => {
            return visit(arg, opts);
        }));
    },
    OptionalCallExpression(node) {
        return UAST.conditionalExpression(visit(node.callee, opts), UAST.literal(null, 'null'), UAST.callExpression(visit(node.callee, opts), node.arguments.map((arg) => {
            return visit(arg, opts);
        })));
    },
    NewExpression(node) {
        return UAST.newExpression(visit(node.callee, opts), node.arguments.map((arg) => {
            return visit(arg, opts);
        }));
    },
    SequenceExpression(node) {
        return UAST.sequence(node.expressions.map((expr) => {
            return visit(expr, opts);
        }));
    },
    ParenthesizedExpression(node) {
        return visit(node.expression, opts);
    },
    DoExpression(node) {
        return visit(node.body, opts);
    },
    ModuleExpression(node) {
        return UAST.scopedStatement(node.body.body.map((stmt) => {
            return visit(stmt, opts);
        }));
    },
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    TopicReference(node) {
        return UAST.noop();
    },
    // TODO
    //  TemplateLiteral
    // TaggedTemplateExpression
    // TemplateElement
    ClassBody() {
        throw new Error('ClassBody should not be visited');
    },
    ClassMethod(node) {
        const res = _visitFunctionLike(node, opts);
        if (node.kind === 'constructor') {
            res._meta.isConstructor = true;
        }
        if (node.async) {
            res._meta.async = true;
        }
        if (node.generator) {
            res._meta.generator = true;
        }
        if (node.static) {
            res._meta.static = true;
        }
        return res;
    },
    ClassPrivateMethod(node) {
        return _visitFunctionLike(node, opts);
    },
    ClassProperty(node) {
        if (node.typeAnnotation && node.typeAnnotation != null) {
            if (node.typeAnnotation.type === "TSTypeAnnotation") {
                const tsTypeAnnotation = node.typeAnnotation;
                if (tsTypeAnnotation?.typeAnnotation?.type === "TSTypeReference") {
                    if ("typeName" in node.typeAnnotation.typeAnnotation && node.typeAnnotation.typeAnnotation?.typeName?.type === "Identifier") {
                        const type = node.typeAnnotation.typeAnnotation?.typeName;
                        return UAST.variableDeclaration(visit(node.key, opts), visit(node.value, opts), false, UAST.scopedType(visit(type, opts), null));
                    }
                }
            }
        }
        return UAST.variableDeclaration(visit(node.key, opts), visit(node.value, opts), false, UAST.dynamicType());
    },
    ClassPrivateProperty(node) {
        if (node.typeAnnotation && node.typeAnnotation != null) {
            if (node.typeAnnotation.type === "TSTypeAnnotation") {
                const tsTypeAnnotation = node.typeAnnotation;
                if (tsTypeAnnotation?.typeAnnotation?.type === "TSTypeReference") {
                    if ("typeName" in node.typeAnnotation.typeAnnotation && node.typeAnnotation.typeAnnotation?.typeName?.type === "Identifier") {
                        const type = node.typeAnnotation.typeAnnotation?.typeName;
                        return UAST.variableDeclaration(visit(node.key, opts), visit(node.value, opts), false, UAST.scopedType(visit(type, opts), null));
                    }
                }
            }
        }
        return UAST.variableDeclaration(visit(node.key, opts), visit(node.value, opts), false, UAST.dynamicType());
    },
    StaticBlock(node) {
        return UAST.scopedStatement(node.body.map((stmt) => visit(stmt, opts)));
    },
    ClassDeclaration(node) {
        const supers = node.superClass ? [visit(node.superClass, opts)] : [];
        return UAST.classDefinition(visit(node.id, opts), node.body.body.map((n) => {
            return visit(n, opts);
        }), supers);
    },
    ClassExpression(node) {
        const supers = node.superClass ? [visit(node.superClass, opts)] : [];
        return UAST.classDefinition(visit(node.id, opts), node.body.body.map((n) => {
            return visit(n, opts);
        }), supers);
    },
    MetaProperty(node) {
        return UAST.objectProperty(visit(node.meta, opts), visit(node.property, opts));
    },
    ImportDeclaration(node) {
        const importExpression = UAST.importExpression(visit(node.source, opts));
        const exportVal = createTmpVariableDeclaration(importExpression);
        const sequence = node.specifiers.map((n) => {
            const specifierDecl = visit(n, opts);
            if (AST.isImportSpecifier(n)) {
                specifierDecl.init = UAST.memberAccess(exportVal.id, visit(n.imported, opts), false);
            }
            else if (AST.isImportDefaultSpecifier(n)) {
                specifierDecl.init = UAST.memberAccess(exportVal.id, UAST.identifier('default'), false);
            }
            else { // ImportNamespaceSpecifier
                specifierDecl.init = exportVal.id;
            }
            return specifierDecl;
        });
        sequence.unshift(exportVal);
        return UAST.sequence(sequence);
    },
    ImportSpecifier(node) {
        return UAST.variableDeclaration(visit(node.local, opts), null, false, UAST.dynamicType());
    },
    ImportDefaultSpecifier(node) {
        return UAST.variableDeclaration(visit(node.local, opts), null, false, UAST.dynamicType());
    },
    ImportNamespaceSpecifier(node) {
        return UAST.variableDeclaration(visit(node.local, opts), null, false, UAST.dynamicType());
    },
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    ImportAttribute(node) {
        throw new Error('ImportAttribute is not implemented yet');
    },
    ExportNamedDeclaration(node) {
        // case 1: export let x = 1;
        if (node.declaration) {
            if ('id' in node.declaration) {
                return UAST.sequence([visit(node.declaration, opts), UAST.exportStatement(visit(node.declaration, opts), visit(node.declaration.id, opts))]);
            }
            else if ('declarations' in node.declaration) {
                return UAST.sequence([visit(node.declaration, opts), UAST.exportStatement(visit(node.declaration.declarations[0].id, opts), visit(node.declaration.declarations[0].id, opts))]);
            }
            throw new Error('ExportNamedDeclaration: declaration should have id or declarations');
        }
        // export ... from 'xx'
        if (node.source) {
            const importVal = createTmpVariableDeclaration(UAST.importExpression(visit(node.source, opts)));
            const sequence = UAST.sequence([]);
            const scopeList = sequence.expressions;
            scopeList.push(importVal);
            node.specifiers.map((specifier) => {
                if (AST.isExportSpecifier(specifier)) {
                    const exportId = visit(specifier.exported, opts);
                    scopeList.push(UAST.variableDeclaration(exportId, UAST.memberAccess(importVal.id, visit(specifier.local, opts)), false, UAST.dynamicType()));
                    scopeList.push(UAST.exportStatement(exportId, exportId));
                }
            });
            return sequence;
        }
        else {
            const seqList = node.specifiers.map((specifier) => {
                if (AST.isExportSpecifier(specifier)) {
                    return UAST.exportStatement(visit(specifier.local, opts), visit(specifier.exported, opts));
                }
                throw new Error('specifier should only be ExportSpecifier');
            });
            return UAST.sequence(seqList);
        }
    },
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    ExportSpecifier(node) {
        throw new Error('ExportSpecifier should not be visited, since it has processed where it occurs');
    },
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    ExportDefaultSpecifier(node) {
        throw new Error('ExportDefaultSpecifier should not be visited, since it has processed where it occurs');
    },
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    ExportNamespaceSpecifier(node) {
        throw new Error('ExportNamespaceSpecifier should not be visited, since it has processed where it occurs');
    },
    ExportDefaultDeclaration(node) {
        const importVal = createTmpVariableDeclaration(visit(node.declaration, opts));
        const scope = UAST.scopedStatement([]);
        const scopeList = scope.body;
        scopeList.push(importVal);
        scopeList.push(UAST.exportStatement(importVal.id, UAST.identifier('default')));
        return scope;
    },
    ExportAllDeclaration(node) {
        // const importVal = createTmpVariableDeclaration(UAST.importExpression(visit(node.source) as UAST.Literal));
        // const tmpVar = createTmpVariableDeclaration(null, false, node);
        // const tmpVarId = tmpVar.id;
        // return UAST.rangeStatement(null, tmpVar, importVal, UAST.exportStatement(tmpVarId, tmpVarId));
        return UAST.spreadElement(UAST.importExpression(visit(node.source, opts)));
    },
    Decorator(node) {
        return visit(node.expression, opts);
    },
    /** typescript node processing **/
    TSAsExpression(node) {
        return visit(node.expression, opts);
    },
    TSNonNullExpression(node) {
        return visit(node.expression, opts);
    },
    TSTypeAssertion(node) {
        return visit(node.expression, opts);
    },
    TSEnumDeclaration(node) {
        const objectProperties = [];
        let lastValue = UAST.literal(0, 'number');
        for (const prop of node.members) {
            const op = visit(prop, opts);
            if (!op.value) {
                op.value = UAST.binaryExpression('+', lastValue, UAST.literal(1, 'number'));
                lastValue = op.value;
            }
            objectProperties.push(op);
        }
        return UAST.objectExpression(objectProperties, visit(node.id, opts));
    },
    TSEnumMember(node) {
        return UAST.objectProperty(visit(node.id, opts), visit(node.initializer, opts));
    },
    // FIXME: 接口内部属性visitor待增加
    TSInterfaceDeclaration(node) {
        return UAST.classDefinition(visit(node.id, opts), [], []);
    },
    TSTypeAliasDeclaration(node) {
        return UAST.noop();
    },
    TSImportEqualsDeclaration(node) {
        const importExpression = UAST.importExpression(visit(node.moduleReference, opts));
        const id = visit(node.id, opts);
        return UAST.variableDeclaration(id, importExpression, false, UAST.dynamicType());
    },
    TSExternalModuleReference(node) {
        return visit(node.expression, opts);
    },
    TSModuleDeclaration(node) {
        return UAST.noop();
    },
    TSParameterProperty(node) {
        return visit(node.parameter, opts);
    },
    TSDeclareMethod(node) {
        return UAST.noop();
    },
    TSInstantiationExpression(node) {
        return visit(node.expression, opts);
    },
    TSDeclareFunction(node) {
        return UAST.noop();
    },
    //TSX
    JSXElement(node) {
        return UAST.noop();
    }
});
function _visitObjectPattern(node, initExpr, opts) {
    const sequence = UAST.sequence([]);
    const exprList = sequence.expressions;
    const consumedKey = [];
    node.properties.forEach((objProperty) => {
        //processObjectProperty
        if (!AST.isRestElement(objProperty)) {
            const { key, value } = objProperty;
            const memberAccess = UAST.memberAccess(initExpr, visit(key, opts));
            consumedKey.push(visit(key, opts));
            if (AST.isIdentifier(value)) {
                const varDecl = UAST.variableDeclaration(visit(value, opts), memberAccess, false, UAST.dynamicType());
                exprList.push(varDecl);
            }
            else if (AST.isPatternLike(value)) { // process pattern like
                exprList.push(..._visitPatternLike(value, memberAccess, opts).expressions);
            }
            else {
                throw new Error(`unexpected type ${value.type}`);
            }
        }
        else { // rest element
            const varDecl = UAST.variableDeclaration(visit(objProperty.argument, opts), initExpr, true, UAST.dynamicType());
            const varId = varDecl.id;
            exprList.push(varDecl);
            consumedKey.forEach((k) => {
                const ma = UAST.memberAccess(varId, k);
                const deleteExpression = UAST.unaryExpression('delete', ma, true);
                exprList.push(deleteExpression);
            });
            const assignmentExpression = UAST.assignmentExpression(visit(objProperty.argument, opts), varId, '=');
            exprList.push(assignmentExpression);
        }
    });
    return sequence;
}
// const [a,b,c] = [a1, b1, c1];
// transformed to:
// const {1:a, 2:b, 3:c} = [a1, b1, c1];
function _visitArrayPattern(node, initExpr, opts) {
    const exprList = [];
    const consumed = [];
    for (let i = 0; i < node.elements.length; i++) {
        const ele = node.elements[i];
        if (ele) {
            if (AST.isRestElement(ele)) {
                const varDecl = createTmpVariableDeclaration(initExpr, true);
                exprList.push(varDecl);
                const varId = varDecl.id;
                for (const c of consumed) {
                    const ma = UAST.memberAccess(varId, c);
                    const deleteExpression = UAST.unaryExpression('delete', ma, true);
                    exprList.push(deleteExpression);
                }
                const assignmentExpression = UAST.assignmentExpression(visit(ele.argument, opts), varId, '=');
                exprList.push(assignmentExpression);
            }
            else {
                const memberAccess = UAST.memberAccess(initExpr, UAST.identifier(String(i)));
                const seq = _visitPatternLike(ele, memberAccess, opts);
                exprList.push(seq);
                consumed.push(UAST.identifier(String(i)));
            }
        }
    }
    return _flatSequence(exprList);
}
function _visitAssignmentPattern(node, initExpr, opts) {
    const condExpr = UAST.conditionalExpression(initExpr, initExpr, visit(node.right, opts));
    return _visitPatternLike(node.left, condExpr, opts);
}
function _visitPatternLike(node, initExpr, opts) {
    if (AST.isIdentifier(node)) {
        return UAST.sequence([UAST.variableDeclaration(visit(node, opts), initExpr, false, UAST.dynamicType())]);
    }
    if (AST.isObjectPattern(node)) {
        return _visitObjectPattern(node, initExpr, opts);
    }
    if (AST.isArrayPattern(node)) {
        return _visitArrayPattern(node, initExpr, opts);
    }
    if (AST.isAssignmentPattern(node)) {
        return _visitAssignmentPattern(node, initExpr, opts);
    }
    if (AST.isMemberExpression(node)) {
        return UAST.sequence([UAST.assignmentExpression(visit(node, opts), initExpr, '=')]);
    }
    if (AST.isRestElement(node)) {
        return UAST.sequence([UAST.assignmentExpression(visit(node.argument, opts), initExpr, '=')]);
    }
}
function _visitFunctionLike(node, opts) {
    const attachBodyList = [];
    // @ts-ignore
    const id = visit(node.key || node.id);
    // for ArrowFunctionExpression
    let body = AST.isExpression(node.body) ? UAST.scopedStatement([UAST.returnStatement(visit(node.body, opts))]) :
        visit(node.body, opts);
    const params = node.params.map((param) => {
        if (AST.isTSParameterProperty(param)) {
            param = param.parameter;
        }
        if (AST.isObjectPattern(param) || AST.isArrayPattern(param) || AST.isAssignmentPattern(param) || AST.isRestElement(param)) {
            const varDecl = createTmpVariableDeclaration();
            if (AST.isRestElement(param)) {
                varDecl._meta.isRestElement = true;
            }
            attachBodyList.push(..._visitPatternLike(param, varDecl.id, opts).expressions);
            varDecl.loc = param.loc;
            return varDecl;
        }
        // type of param is Identifier
        const id = visit(param, opts);
        const decl = UAST.variableDeclaration(id, null, false, UAST.dynamicType());
        decl.loc = id.loc;
        return decl;
    });
    attachBodyList.push(body);
    if (attachBodyList.length > 0) {
        body = UAST.scopedStatement([...attachBodyList]);
    }
    const res = UAST.functionDefinition(id, params, UAST.dynamicType(), body, []);
    return res;
}
function _visitRangeStatement(node, opts) {
    const right = visit(node.right, opts);
    //TODO this should be a iterator
    const keyOrValue = createTmpVariableDeclaration(right);
    keyOrValue.loc = node.left.loc;
    let body = visit(node.body, opts);
    if (AST.isVariableDeclaration(node.left)) {
        // 有且仅能有一个decl
        const seq = _visitPatternLike(node.left.declarations[0].id, keyOrValue.id, opts);
        const seqExpressions = [seq, body];
        body = UAST.scopedStatement(seqExpressions);
    }
    else if (AST.isObjectPattern(node.left) || AST.isArrayPattern(node.left) || AST.isAssignmentPattern(node.left)) {
        const seq = _visitPatternLike(node.left, keyOrValue.id, opts);
        const seqExpressions = [seq, body];
        body = UAST.scopedStatement(seqExpressions);
    }
    else {
        const expr = UAST.sequence([UAST.assignmentExpression(visit(node.left, opts), keyOrValue.id, '=')]);
        const seqExpressions = [expr, body];
        body = UAST.scopedStatement(seqExpressions);
    }
    if (AST.isForInStatement(node)) {
        return UAST.rangeStatement(keyOrValue, null, right, body);
    }
    else {
        return UAST.rangeStatement(null, keyOrValue, right, body);
    }
}
//扁平化优化, 减少sequence层次
function _flatSequence(node) {
    const sequenceArray = [];
    const res = UAST.sequence(sequenceArray);
    if (Array.isArray(node)) {
        for (const n of node) {
            const subSequence = _flatSequence(n);
            sequenceArray.push(...subSequence.expressions);
        }
        return res;
    }
    else if (UAST.isSequence(node)) {
        return node;
    }
    else {
        sequenceArray.push(node);
        return res;
    }
}
function createTmpIdentifier(locNode) {
    if (!getUid) {
        getUid = (() => {
            let id = 0;
            return () => id++;
        })();
    }
    const id = UAST.identifier(`__tmp${getUid()}__`);
    return id;
}
exports.createTmpIdentifier = createTmpIdentifier;
function createTmpVariableDeclaration(init, cloned = false, locNode) {
    locNode = locNode ? locNode : init;
    return UAST.variableDeclaration(createTmpIdentifier(locNode), init, cloned, UAST.dynamicType());
}
exports.createTmpVariableDeclaration = createTmpVariableDeclaration;
function appendLocMeta(dest, src) {
    if (Array.isArray(src)) {
        if (src.length > 0) {
            dest.loc = src[0].loc;
            dest.loc.start = src[0].loc.start;
            dest.loc.end = src[src.length - 1].loc.end;
            if ('sourcefile' in src[0].loc) {
                dest.loc.sourcefile = src[0].loc.sourcefile;
            }
            else if ('filename' in src[0].loc) {
                dest.loc.sourcefile = src[0].loc.filename;
            }
        }
    }
    else {
        // @ts-ignore
        dest.loc = src.loc;
    }
    dest._meta.loc = dest.loc;
    return dest;
}
exports.appendLocMeta = appendLocMeta;
// append loc to node recursively
function appendLocRec(node, parent_node, sourcefile) {
    function mergeLocs(locs) {
        if (!locs)
            return null;
        let minLine = Number.MAX_SAFE_INTEGER, minColumn = Number.MAX_SAFE_INTEGER, maxLine = 0, maxColumn = 0;
        locs.forEach(loc => {
            if (!loc)
                return;
            minLine = Math.min(minLine, loc.start.line);
            minColumn = Math.min(minColumn, loc.start.column);
            maxLine = Math.max(maxLine, loc.end.line);
            maxColumn = Math.max(maxColumn, loc.end.column);
        });
        return {
            start: { line: minLine, column: minColumn },
            end: { line: maxLine, column: maxColumn },
            sourcefile: sourcefile
        };
    }
    if (!node)
        return null;
    if (Array.isArray(node)) {
        const locs = node.map(v => {
            return appendLocRec(v, parent_node, sourcefile);
        });
        return mergeLocs(locs);
    }
    if (!node.type)
        return null;
    const locs = [];
    for (const prop in node) {
        if (['type', 'loc', '_meta', 'varType', 'operator'].indexOf(prop) !== -1) {
            continue;
        }
        const loc = appendLocRec(node[prop], node.loc ? node : parent_node, sourcefile);
        if (loc) {
            locs.push(loc);
        }
    }
    if (!node.loc) {
        if (parent_node?.loc) {
            node.loc = parent_node.loc;
            if (!node.loc.sourcefile) {
                node.loc.sourcefile = sourcefile;
            }
        }
        else {
            node.loc = mergeLocs(locs);
        }
    }
    return node.loc;
}
// function visit(ast: AST.Node & AST.Node[] | {type?:any} = {}): ParseResult<UAST.ASTNode | UAST.ASTNode[] > {
function visit(orig, opts) {
    if (!orig) {
        // @ts-ignore
        return null;
    }
    // @ts-ignore
    const mVisitor = visitor(opts);
    if (typeof mVisitor[orig.type] !== 'function') {
        const noop = UAST.noop();
        appendLocRec(noop, orig, opts?.sourcefile);
        noop._meta.origin = orig; // 如果没有找到对应节点的visitor， 将原始ast节点记录到meta.origin中
        return noop;
    }
    // @ts-ignore
    const node = mVisitor[orig.type](orig);
    appendLocRec(node, orig, opts?.sourcefile);
    if (UAST.isNode(node)) {
        if (node.loc) {
            // @ts-ignore
            delete node.loc.start.index;
            // @ts-ignore
            delete node.loc.end.index;
            Object.keys(node.loc).forEach(key => {
                if (!['start', 'end', 'sourcefile'].includes(key)) {
                    // @ts-ignore
                    delete node.loc[key];
                }
            });
        }
        const metaProperties = ['start', 'end', 'range', 'leadingComments', 'trailingComments', 'innerComments', 'directives', 'interpreter'];
        if (UAST.isNoop(node)) {
            node._meta.origin = orig;
        }
        for (const prop of metaProperties) {
            if (orig && orig[prop]) {
                node._meta._extra = node._meta._extra || {};
                const extra = node._meta._extra;
                extra[prop] = orig[prop];
            }
        }
    }
    // process decorators
    if ("decorators" in orig) {
        const decorators = [];
        orig.decorators.forEach(decorator => {
            decorators.push(visit(decorator, opts));
        });
        node._meta.decorators = decorators;
    }
    return node;
}
function fix_column(node) {
    if (!node)
        return;
    if (Array.isArray(node)) {
        node.forEach(function (n) {
            fix_column(n);
        });
    }
    if (!node.loc)
        return;
    //uast统一 column从1开始，babel从0开始，因此整体+1
    if (node.loc.start?.column >= 0 && node.loc.start.hasFixColumn === undefined) {
        node.loc.start.column = node.loc.start.column + 1;
        node.loc.start.hasFixColumn = true;
    }
    if (node.loc.end?.column >= 0 && node.loc.end.hasFixColumn === undefined) {
        node.loc.end.column = node.loc.end.column + 1;
        node.loc.end.hasFixColumn = true;
    }
    Object.keys(node).forEach(key => {
        fix_column(node[key]);
    });
}
function sanitize(node) {
    if (!node)
        return;
    if (Array.isArray(node)) {
        node.forEach(function (n) {
            sanitize(n);
        });
    }
    if (!node.type)
        return;
    // clean up loc
    if (node.loc) {
        // @ts-ignore
        delete node.loc.start.index;
        delete node.loc.end.index;
        delete node.loc.start.hasFixColumn;
        delete node.loc.end.hasFixColumn;
        Object.keys(node.loc).forEach(key => {
            if (!['start', 'end', 'sourcefile'].includes(key)) {
                // @ts-ignore
                delete node.loc[key];
            }
        });
    }
    Object.keys(node).forEach(key => {
        sanitize(node[key]);
    });
}
let getUid;
function parse(content, opts) {
    //init engine state
    getUid = (() => {
        let id = 0;
        return () => id++;
    })();
    let ast;
    try {
        ast = tryBabelCore(content, opts);
    }
    catch (e) {
        ast = tryBabelParse(content, opts);
    }
    const node = visit(ast, opts);
    fix_column(node);
    sanitize(node);
    return node;
}
exports.parse = parse;
function tryBabelParse(content, opts) {
    return babelParser.parse(content, {
        sourceFilename: opts?.sourcefile,
        sourceType: 'module',
        allowImportExportEverywhere: true,
        allowAwaitOutsideFunction: true,
        allowReturnOutsideFunction: true,
        allowSuperOutsideMethod: true,
        allowUndeclaredExports: true,
        attachComment: true,
        errorRecovery: true,
        createParenthesizedExpressions: false,
        plugins: [
            "asyncDoExpressions",
            "asyncGenerators",
            "bigInt",
            "classPrivateMethods",
            "classPrivateProperties",
            "classProperties",
            "classStaticBlock" // Enabled by default
            ,
            "decimal",
            "decorators",
            ["decorators", { decoratorsBeforeExport: true }],
            "decoratorAutoAccessors",
            "destructuringPrivate",
            "doExpressions",
            "dynamicImport",
            "exportDefaultFrom",
            "exportNamespaceFrom" // deprecated
            // , "decorators-legacy"
            // , "flow"
            // , "estree"
            ,
            "flowComments",
            "functionBind",
            "functionSent",
            "importMeta",
            "jsx",
            "logicalAssignment",
            "importAssertions",
            "moduleBlocks",
            "moduleStringNames",
            "nullishCoalescingOperator",
            "numericSeparator",
            "objectRestSpread",
            "optionalCatchBinding",
            "optionalChaining",
            "partialApplication"
            // , "pipelineOperator"
            // , ["pipelineOperator", { proposal: "minimal" }]
            ,
            "placeholders",
            "privateIn" // Enabled by default
            ,
            "recordAndTuple",
            "regexpUnicodeSets",
            "throwExpressions",
            "topLevelAwait",
            "typescript",
            ["typescript", { dts: true }]
            // , "v8intrinsic"
        ],
    });
}
function tryBabelCore(content, opts) {
    return babelCore.parse(content, {
        filename: opts?.sourcefile,
        sourceType: 'module',
        plugins: [
            "@babel/plugin-proposal-optional-chaining",
            "@babel/plugin-syntax-json-strings",
            "@babel/plugin-syntax-export-default-from",
            "@babel/plugin-syntax-jsx",
            "@babel/plugin-transform-react-jsx",
            ["@babel/plugin-proposal-decorators", {
                    decoratorsBeforeExport: true,
                    legacy: false
                }],
        ],
        parserOpts: {
            errorRecovery: true,
        },
        presets: [
            ["@babel/preset-typescript", {
                    allExtensions: true,
                    // isTSX:false,
                }],
            "@babel/preset-react"
        ],
    });
}
//# sourceMappingURL=parser.js.map