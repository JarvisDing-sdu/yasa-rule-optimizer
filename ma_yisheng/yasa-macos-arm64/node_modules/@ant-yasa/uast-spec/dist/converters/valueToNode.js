"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _isValidIdentifier = _interopRequireDefault(require("../validators/isValidIdentifier"));
var _generated = require("../builders/generated");
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
var _default = exports.default = valueToNode;
const objectToString = Function.call.bind(Object.prototype.toString);
function isRegExp(value) {
  return objectToString(value) === "[object RegExp]";
}
function isPlainObject(value) {
  if (typeof value !== "object" || value === null || Object.prototype.toString.call(value) !== "[object Object]") {
    return false;
  }
  const proto = Object.getPrototypeOf(value);
  // Object.prototype's __proto__ is null. Every other class's __proto__.__proto__ is
  // not null by default. We cannot check if proto === Object.prototype because it
  // could come from another realm.
  return proto === null || Object.getPrototypeOf(proto) === null;
}
function valueToNode(value) {
  // undefined
  if (value === undefined) {
    return (0, _generated.identifier)("undefined");
  }

  // boolean
  if (value === true || value === false) {
    return (0, _generated.literal)(value, 'boolean');
  }

  // null
  if (value === null) {
    return (0, _generated.literal)(value, 'null');
  }

  // strings
  if (typeof value === "string") {
    return (0, _generated.literal)(value, 'string');
  }

  // numbers
  if (typeof value === "number") {
    let result;
    if (Number.isFinite(value)) {
      result = (0, _generated.literal)(Math.abs(value), 'number');
    } else {
      let numerator;
      if (Number.isNaN(value)) {
        // NaN
        numerator = (0, _generated.literal)(0, 'number');
      } else {
        // Infinity / -Infinity
        numerator = (0, _generated.literal)(1, 'number');
      }
      result = (0, _generated.binaryExpression)("/", numerator, (0, _generated.literal)(0, 'number'));
    }
    if (value < 0 || Object.is(value, -0)) {
      result = (0, _generated.unaryExpression)("-", result);
    }
    return result;
  }

  // // regexes
  // if (isRegExp(value)) {
  //   const pattern = value.source;
  //   const flags = value.toString().match(/\/([a-z]+|)$/)[1];
  //   return literal(pattern, flags);
  // }

  // array
  if (Array.isArray(value)) {
    return (0, _generated.objectExpression)(value.map((v, index) => {
      return (0, _generated.objectProperty)((0, _generated.literal)(index, 'number'), valueToNode(v));
    }));
  }

  // object
  if (isPlainObject(value)) {
    const props = [];
    for (const key of Object.keys(value)) {
      let nodeKey;
      if ((0, _isValidIdentifier.default)(key)) {
        nodeKey = (0, _generated.identifier)(key);
      } else {
        nodeKey = (0, _generated.literal)(key, 'string');
      }
      props.push((0, _generated.objectProperty)(nodeKey, valueToNode(value[key])));
    }
    return (0, _generated.objectExpression)(props);
  }
  throw new Error("don't know how to turn this value into a node");
}