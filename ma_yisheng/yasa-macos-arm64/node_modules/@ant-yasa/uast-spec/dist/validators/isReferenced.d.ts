import type * as t from "../ast-types/generated";
/**
 * Check if the input `node` is a reference to a bound variable.
 */
export default function isReferenced(node: t.Node, parent: t.Node, grandparent?: t.Node): boolean;
