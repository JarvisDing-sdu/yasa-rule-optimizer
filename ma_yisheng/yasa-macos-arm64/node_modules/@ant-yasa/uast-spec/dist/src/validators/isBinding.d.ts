import type * as t from "../ast-types/generated";
/**
 * Check if the input `node` is a binding identifier.
 */
export default function isBinding(node: t.Node, parent: t.Node, grandparent?: t.Node): boolean;
