import type * as t from "../ast-types/generated";
export type TraversalAncestors = Array<{
    node: t.Node;
    key: string;
    index?: number;
}>;
export type TraversalHandler<T> = (this: undefined, node: t.Node, parent: TraversalAncestors, state: T) => void;
export type TraversalHandlers<T> = {
    enter?: TraversalHandler<T>;
    exit?: TraversalHandler<T>;
};
/**
 * A general AST traversal with both prefix and postfix handlers, and a
 * state object. Exposes ancestry data to each handler so that more complex
 * AST data can be taken into account.
 */
export default function traverse<T>(node: t.Node, handlers: TraversalHandler<T> | TraversalHandlers<T>, state?: T): void;
