import type * as t from "../ast-types/generated";
/**
 * Add comment of certain type to a node.
 */
export default function addComment<T extends t.Node>(node: T, type: string, content: string, line?: boolean): T;
