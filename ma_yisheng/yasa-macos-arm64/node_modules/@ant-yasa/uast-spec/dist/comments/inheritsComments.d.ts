import type * as t from "../ast-types/generated";
/**
 * Inherit all unique comments from `parent` node to `child` node.
 */
export default function inheritsComments<T extends t.Node>(child: T, parent: t.Node): T;
